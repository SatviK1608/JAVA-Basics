package array.basics;

/*
 * fill only works with 1D array
 * Arrays.fill(array,value)  -->entire array will have same value
 * Arrays.fill(array,fromIndex,toIndex,value); -->array b/w range of indices will have same value
 */

/*
 * Arrays.binarySearch(array,key);
 * Arrays.binarySearch(array,fromIndex,toIndex,key);
 */

/*
 * Arrays.equals(array1,array2)   //boolean
 * Arrays.equals(array1,fromIndex1,toIndex1,array2,fromIndex2,toIndex2)   //boolean
 */

//Arrays.rangeCheck();

//Arrays.toString(array);          //returns string representation of array
public class ArraysClassMethods {

	public ArraysClassMethods() {
		// TODO Auto-generated constructor stub
	}

}
